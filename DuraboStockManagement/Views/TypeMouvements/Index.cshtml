@model IEnumerable<DuraboStockManagement.Models.TypeMouvement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Designation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sens)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Designation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sens)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeMouvementID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeMouvementID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeMouvementID })
        </td>
    </tr>
}

</table>
